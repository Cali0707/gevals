kind: Agent
metadata:
  name: "llm-agent"
builtin:
  type: "llm-agent"
  model: "openai:gpt-4"  # Change to your provider:model (e.g. anthropic:claude-sonnet-4-20250514)
# Before running, set the provider-specific environment variable:
#   export OPENAI_API_KEY="sk-..."
# Or for custom endpoints:
#   export OPENAI_BASE_URL="https://your-endpoint/v1"
